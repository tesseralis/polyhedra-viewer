version: 2.1
orbs:
  node: circleci/node@3.0.1
jobs:
  lint:
    executor:
      name: node/default
    steps:
      - checkout
      - node/install:
          install-yarn: true
          node-version: latest
      - run: yarn lint
  test:
    executor:
      name: node/default
    steps:
      - checkout
      - node/install:
          install-yarn: true
          node-version: latest
      - run: yarn test --runInBand
workflows:
  build-and-test:
    jobs:
      - lint
      - test
